get_filename_component(DIR ${CMAKE_CURRENT_LIST_DIR} NAME)
project(${DIR})
add_executable(${DIR} main.cpp LightDepth_vshader.glsl LightDepth_fshader.glsl ShadowMap_vshader.glsl ShadowMap_fshader.glsl cube.h)
target_link_libraries(${DIR} ${COMMON_LIBS})
target_stringify_shader(${DIR} LightDepth_fshader)
target_stringify_shader(${DIR} LightDepth_vshader)
target_stringify_shader(${DIR} ShadowMap_fshader)
target_stringify_shader(${DIR} ShadowMap_vshader)
target_deploy_file(${DIR} tangle_cube.obj)
target_deploy_file(${DIR} cube.tga)
target_deploy_file(${DIR} rubik.tga)
target_deploy_file(${DIR} rainbow.tga)